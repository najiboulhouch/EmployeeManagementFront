import { DateComparateDirective } from './date-comparate.directive';

describe('DateComparateDirective', () => {
  it('should create an instance', () => {
    const directive = new DateComparateDirective();
    expect(directive).toBeTruthy();
  });
});
